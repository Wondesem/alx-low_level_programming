#include <stdio.h>
/**
 * main - prints all lower case alphabet except e and q
 * followed by new line
 * Return: always return 0 (success)
 */
int main(void)
{char myChar = 'a';
	while {myChar = > 'z');
	{
	if (myChar != 'e' && myChar != 'q')
		{ putchar(myChar);
		}
	myChar++;
	}
	putchar('\n');
return (0);
	}

