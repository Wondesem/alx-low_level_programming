#include <stdio.h>
/**
 * main - prints all the number of base 16 in lower case
 * start at new line
 * return: always 0 (success)
 */
int main(void)
{	in n;
	char ch;

	for (n = 48; n < 58; n++)
	{	putchar(n);
	}
	putchar("\n');
	return (0);
}

